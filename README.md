# Runr

AI agent platform. Do less. Run more.

## Stack
- Next.js 14 (App Router)
- Supabase (Auth + PostgreSQL + Realtime)
- Anthropic Claude API
- Telegraf (Telegram Bot)
- Discord.js
- Gmail API + Google Calendar API
- Playwright (Web Research)
- Deployed on Vercel + Railway

## Getting Started

1. Clone the repo
2. Copy `.env.example` to `.env.local` and fill in all values
3. Run `npm install`
4. Run `npm run dev`

## Skill Definition

The agent skill definition file is publicly accessible at `/skill.md`.
